some garbage ?? asdsd

const legitimate = (y) => {
  return 'Fancy but ignored' + y;
}

const g = (): ?wont make sense => {

  const h = () => {
    I has magic skills
  }

  const f = (x) => {
    return x;
  };
}

some oher gargbae ?? asdds

const ob = {
  y: has garbage,
  z: (x) => {
    return x
  },
  m: more gargoyle
}
